# Slice 2.6: Radix-backed shadcn Alert Dialog primitive

## Status and outcome

**Completed September 12, 2026. Parent: main spec 2. Depends on 2.1.** The
official Alert Dialog source is installed without migrating a destructive
consumer yet.

## Work

- Add `alert-dialog.tsx` through the pinned registry workflow.
- Retain upstream role, title, description, action/cancel, portal, focus, and
  modal behavior. Add only token-based size and destructive styling.
- Support accepted checkbox/summary content through composition rather than a
  custom confirmation abstraction.

## Verification

Tests cover initial focus on the safe action, labelled semantics, Tab containment,
Escape/outside policy, action/cancel focus return, destructive styling, and a
busy controlled-open example that rejects close attempts.

## Implementation evidence

- The shadcn `4.21.0` Alert Dialog source and Radix dependency `1.1.23` are
  installed, pinned, and recorded in the registry manifest.
- The source retains upstream composition and behavior while using accepted
  theme tokens, Button variants, viewport centering, and portal motion.
- Three focused tests cover labelled alert semantics, safe initial focus, Tab
  containment, outside interaction, focus return, and controlled busy state.
