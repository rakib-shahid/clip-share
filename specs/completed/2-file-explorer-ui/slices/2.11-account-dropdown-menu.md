# Slice 2.11: Account Dropdown Menu

## Status and outcome

**Completed September 12, 2026. Parent: main spec 2. Depends on 2.1.** The
handwritten account popup and manual menu roles are replaced by Dropdown Menu.

## Work

- Add the official Radix-backed Dropdown Menu source.
- Render username/role as the trigger content and Change password/Log out as
  labelled icon items in their current order.
- Preserve responsive labels, logout behavior, password-dialog opening, and
  focus return. Remove `accountOpen` and manual absolute-position/menu markup.

## Verification

Test pointer and keyboard open, Arrow/Home/End navigation, Escape, outside close,
action invocation once, focus restoration, and narrow layout. Existing auth and
password tests remain unchanged.

## Implementation evidence

- The official Dropdown Menu source and Radix dependency `2.1.24` are pinned and
  recorded in the registry manifest.
- Username/role, Change password, and Log out retain their content, order, and
  behavior; `accountOpen` and handwritten menu roles are removed.
- Focused tests cover keyboard open, initial focus, End navigation, Escape/focus
  return, password opening, and exactly-once logout.
