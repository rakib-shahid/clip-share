# Slice 2.32: Final cleanup and acceptance pass

## Status and outcome

**Completed September 13, 2026. Parent: main spec 2. Depends on all prior 2.x slices.** Removed superseded
custom UI code and prove the complete main-spec contract in supported deployment.

## Work

- Remove `ui/modal.tsx`, `ItemManagementBar`, handwritten account menu, custom
  breadcrumb/empty/skeleton/progress implementations, dead state/helpers, and
  dependencies made unused by migration. Do not remove a compatibility wrapper
  until `rg` and tests prove no consumer.
- Reconcile registry manifest, pinned packages/lockfile, component imports,
  theme tokens, bundle delta, specs, and development notes.
- Add focused Playwright paths for normal/admin grid/list, every sort across more
  than 60 items, navigation races, menus/context menus, responsive Drawer,
  destructive dialogs, upload Attachment, editor typing, and reduced motion.
- Run the complete suite in desktop Chromium. Mark critical Dialog, Alert Dialog,
  Drawer, Dropdown/Context Menu, navigation, layout, and reduced-motion paths
  `@cross-browser` and run them in desktop Firefox and WebKit. Mark the visible
  touch-menu/Drawer/320 px paths `@touch` and run them in a Pixel 7-emulated
  Chromium project; right-click is not required in that touch project.
- Confirm the registry manifest records shadcn CLI `4.21.0`, `motion` is exactly
  `13.1.1`, and the lockfile contains no unintended duplicate primitive stack.

## Verification

Run lint, React tests/build, Go tests, the exact Chromium/Firefox/WebKit/touch
matrix above with accessibility/keyboard coverage, and Docker Compose runtime
checks. `git diff --check` passes;
no manual `role="dialog"`/`role="menu"`, focus trap, or avoidable replacement
primitive remains. Record any justified custom interaction in the main spec.
